create table Product(
id bigint not null,
primary key(id)
) engine=InnoDB;

create table ProductCategory(
id bigint not null,
primary key(id)
) engine=InnoDB;

create table Person(
id bigint not null,
fName varchar()
primary key(id)
) engine=InnoDB;